---
title: Set
description: Hash set with insertion, lookup and removal methods.
---

import { Badge, Card } from '@astrojs/starlight/components';

**Module:** <Badge text="std.collections.set" variant="note" size="medium" /> <br/>
**Source:** [std/collections/set.zn](https://github.com/mealet/zeen/blob/main/lib/std/collections/set.zn) <br/>

## Structures
<Card title="HashSet[T: Hash + Eq]">
  Separate-chaining hash set, load factor 0.75

  ----

  **Fields:**
  ```zeen
  buckets: List[List[SetEntry[T]]],
  _len: usize,
  _capacity: usize,
  ```
  **Constructors:**
  ```zeen
  pub fn new() Self;
  ```
  ```zeen
  pub fn with_capacity(cap: usize) Self;
  ```

  **Getters:**
  ```zeen
  pub fn len(*const self) usize;
  ```
  ```zeen
  pub fn capacity(*const self) usize;
  ```
  ```zeen
  pub fn is_empty(*const self) bool;
  ```
  ```zeen
  pub fn contains(*const self, value: *const T) bool;
  ```
  ```zeen
  pub fn get(*const self, value: *const T) Option[*const T];
  ```

  **Mutations:**
  ```zeen
  pub fn insert(*self, value: T) bool;
  ```
  ```zeen
  pub fn remove(*self, value: *const T) bool;
  ```
  ```zeen
  pub fn clear(*self);
  ```

  **Interfaces:**
  - `core.ops` : `Drop`, `Clone` (when `T: Clone`)
</Card>

<Card title="SetEntry[T]">
  Single set entry

  ----

  **Fields:**
  ```zeen
  pub hash: u64,
  pub key: T,
  ```
</Card>
